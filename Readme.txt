1st Step : Open a terminal or command prompt in the project folder and run the "npm install" command install project dependencies.
2nd Step : Once the dependencies are installed,you can start the development server using the "npm start" command.
